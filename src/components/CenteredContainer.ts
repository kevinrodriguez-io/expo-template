import styled from 'styled-components/native'

export const CenteredContainer = styled.View({
  flex: 1,
  alignItems: 'center',
  justifyContent: 'center',
})

export const CenteredContainerFlexEnd = styled.View({
  flex: 1,
  alignItems: 'center',
  justifyContent: 'flex-end',
})
